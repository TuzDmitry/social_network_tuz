

let initialState={
    friends: [
        {id: '1', name: 'Andrey'},
        {id: '2', name: 'Sasha'},
        {id: '3', name: 'Sveta'}
    ]
}

const navBarReducer=(state=initialState, action)=>{
    // switch (action.type) {
    //
    // }

    return state;
}


export default navBarReducer;