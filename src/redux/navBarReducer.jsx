

let initialState={
    friends: [
        {id: '1', name: 'Andrey'},
        {id: '1', name: 'Sasha'},
        {id: '1', name: 'Sveta'}
    ]
}

const navBarReducer=(state=initialState, action)=>{
    // switch (action.type) {
    //
    // }

    return state;
}


export default navBarReducer;